// Placeholder to ensure SPM creates a framework for this FFI plugin.
// The Rust native library (cdylib) is built by cargokit and bundled
// via Flutter's ffiPlugin build system, not compiled by SPM.
